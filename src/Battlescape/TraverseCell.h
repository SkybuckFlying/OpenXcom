#pragma once

template <typename FPT>
struct TraverseCell
{
	FPT mWidth, mHeight, mDepth;

	void Set( FPT ParaWidth, FPT ParaHeight, FPT ParaDepth );
};

template <typename FPT>
void TraverseCell<FPT>::Set( FPT ParaWidth, FPT ParaHeight, FPT ParaDepth )
{

}
